# P0 — 地基

- 状态：**进行中**
- 开始日期：2026-09-18
- 完成日期：

---

## 目标

建立标准化的工程规范与可变现前的最小可用框架：能扫描 `tools/` 目录 → 入库 → 前台展示 → 后台 CRUD。

**不做**：网盘中间页、统计注入、离线包（属 P1/P3）。**不开发工具**（属 P2）。

---

## 前置条件

- [x] 需求文档 v2.1 冻结（`docs/需求文档-v2.md`）
- [x] 工程规范文件建立（`AGENTS.md` / `README.md` / `.gitignore` / `.editorconfig` / `.env.example`）
- [x] `docs/` 规范文档集建立
- [x] 目录骨架建立
- [x] git 初始化
- [x] PHP 8+ 与 Python 3 本地环境可用（PHP 8.4.24 / Python 3.14.6，2026-09-18 确认）

---

## 任务清单

### 一、工程基础设施

- [x] `.gitignore` / `.editorconfig` / `.env.example`
- [x] `AGENTS.md` / `README.md` / `CHANGELOG.md` / `THIRD-PARTY-LICENSES.md`
- [x] `docs/` 文档集：需求文档 v2.1、架构说明、工具开发规范、manifest 规范、编码规范、安全规范、图标与许可证、工具例外清单
- [x] `tasks/` 任务计划表（P0-P5）
- [x] `var/` 隔离区（cache / tmp / logs / exports）
- [x] git 初始化与首次提交
- [x] git pre-commit 钩子（跑四项校验，`core.hooksPath=.githooks` 已配置）

### 二、设计令牌与 CSS 基础

- [x] `assets-src/css/tokens.css` — 填全具体数值（色板 / 字号 / 间距 / 圆角 / 阴影 / 层级）
- [x] `assets-src/css/base.css` — 重置 + 基础排版
- [x] `assets-src/css/components.css` — 按钮 / 卡片 / 表格 / 表单 / 标签 / 分页 / 弹层 / 提示
- [x] `assets-src/css/site.css` — 前台布局
- [x] `assets-src/icons/icons.txt` — 初始图标清单
- [x] 构建脚本 `scripts/build_css.py`（含 `--check` 模式）
- [ ] 构建产出：`public/assets/css/*.css`（已完成）+ `public/assets/icons/sprite.svg`（待图标源）

### 三、自研框架核心（`src/core/`）

- [ ] `Env.php` — `.env` 读取
- [ ] `Config.php` — 配置读取（Env > DB > 默认）
- [ ] `Database.php` — PDO 封装 + PRAGMA（WAL / busy_timeout / foreign_keys）
- [ ] `Router.php` — 路由（支持 `/tool/{id}` 参数占位）
- [ ] `Request.php` / `Response.php`
- [ ] `View.php` — 模板渲染 + `e()` 转义辅助
- [ ] `Session.php` — 会话管理（自定义名 / HttpOnly / SameSite）
- [ ] `Csrf.php` — token 生成与校验
- [ ] `Security.php` — `safePath()` / 路径校验 / IP 获取
- [ ] `Logger.php` — 写 `var/logs/`
- [ ] `bootstrap.php` — 应用引导 + 生产环境启动自检
- [ ] 统一异常处理（生产不泄露堆栈）

### 四、数据库

- [ ] `scripts/init_db.php` — 建库建表（`storage/app.db` + `storage/stats.db`）
- [ ] 表：`categories`（两级）
- [ ] 表：`tools`（manifest 镜像 + 用户态字段）
- [ ] 表：`tool_category`
- [ ] 表：`tool_netdisks`
- [ ] 表：`stats`
- [ ] 表：`admins`
- [ ] 表：`site_config`
- [ ] 表：`login_attempts`（登录限流）
- [ ] 表：`tool_overrides`（`tools/` 不可写时的降级方案）
- [ ] 初始数据：学段 / 学科分类种子

### 五、工具扫描与 manifest

- [ ] `services/ToolScanner.php` — 扫描 + 校验 + upsert（保留用户态字段）
- [ ] `services/ManifestWriter.php` — manifest 回写（含不可写降级）
- [ ] `scripts/check_manifest.py` — manifest 完整性 + `ET-META` 一致性校验
- [ ] `scripts/check_no_external.py` — 外部域名检查
- [ ] `scripts/check_css_tokens.py` — CSS 字面量检查
- [ ] `scripts/sync_shared.py` — 共享逻辑同步（含 `--check`）
- [ ] `scripts/icons/build_sprite.py` — 图标子集化
- [ ] `tools/_shared/` 目录与 `shared.manifest.json` 骨架

### 六、前台（`src/frontend/`）

- [ ] 布局模板（头部 / 导航 / 页脚）
- [ ] 首页 `/` — 推荐工具 + 分类导航 + 搜索入口
- [ ] 分类页 `/category/{slug}`
- [ ] 搜索页 `/search?q=`
- [ ] 工具详情页 `/tool/{id}` — 介绍 + 标签 + 版本号（iframe 预览与获取按钮留待 P1）

### 七、后台（`src/admin/`）

- [ ] `AuthController` — 登录 / 登出（含限流）
- [ ] `AuthMiddleware` / `RoleMiddleware` / `CsrfMiddleware`
- [ ] 后台布局模板（侧栏 + 顶栏）
- [ ] 仪表盘 — 工具数 / 分类数 / 快捷入口
- [ ] 工具管理 — 列表 / 扫描同步 / 编辑元数据（回写 manifest）/ 启用禁用 / 排序 / 推荐 / **版本不一致高亮**
- [ ] 分类管理 — 两级 CRUD / 排序
- [ ] 站点设置 — 站点名 / SEO / 下载开关
- [ ] 系统信息 — 配置回显（敏感项掩码）/ 写入权限检测（`tools/` 是否可写）
- [ ] 无密码后台支持（IP 白名单 + 醒目警告条）

### 八、验证与提交

- [ ] 本地跑通：`php -S localhost:8000 -t public` → 首页可访问
- [ ] 后台可登录 → 扫描同步 → 工具入库 → 前台可见
- [ ] `python scripts/check_manifest.py` 通过
- [ ] `python scripts/check_no_external.py` 通过
- [ ] `python scripts/sync_shared.py --check` 通过
- [ ] 放置 1 个示例工具（`tools/_demo/`）验证扫描链路
- [ ] 提交并打 tag `v0.1.0-p0`

---

## 验收标准

（P0 完成的客观判据）

1. **工程规范**：`AGENTS.md` + `docs/` 8 份文档 + `tasks/` 6 份任务单齐备
2. **框架可用**：`php -S localhost:8000 -t public` 启动后首页 200
3. **扫描链路**：放入一个新工具目录 → 后台点"扫描同步" → 前台列表出现 → 详情页可访问
4. **manifest 回写**：后台改工具标题 → `tools/{id}/manifest.json` 文件内容变更 → git diff 可见
5. **版本一致性检测**：故意改错 `ET-META` 版本 → 后台列表该行高亮报错
6. **后台认证**：未登录访问 `/admin/tools` 跳登录页；`editor` 角色访问站点设置返回 403
7. **零外部依赖**：`check_no_external.py` 退出码 0
8. **临时产物隔离**：`git status` 中无 `var/`、无 `*.db`、无 `.env`
9. **CSS 令牌**：`check_css_tokens.py` 退出码 0（`base/components/site` 无字面量）
10. **生产自检**：故意设 `APP_ENV=production` + `APP_DEBUG=true` → 启动报错并退出

---

## 风险与备注

### 已识别风险

| 风险 | 应对 |
|---|---|
| 自写 CSS 拖慢进度 | §3.1 兜底：切 Tailwind 本地构建产物，只换 `assets/css/` 产出。**P1 结束前必须决定** |
| `tools/` 目录不可写 | 降级写 `tool_overrides` 表，后台提示降级模式 |
| 设计令牌数值未定 | 参考 `docs/需求文档-v2.md` §7.5 骨架，编码前补全 |
| 图标集本地离线包获取 | `build_sprite.py` 需本地图标源；若获取困难，先手工整理用到的 SVG |

### 待决策

- [ ] `assets-src/` → `public/assets/` 是否需要构建步骤，还是直接同目录（手写 CSS 无编译，可考虑直接放 `public/`）
- [ ] 图标离线源的组织方式（本地缓存目录 or 内置 iconify JSON）
- [ ] 是否需要 CSRF 之外的 `SameSite` 之外保护（如双提交 Cookie）

### 进度记录

| 日期 | 进展 |
|---|---|
| 2026-09-18 | 需求文档升 v2.1；工程规范文件与 `docs/`/`tasks/`/目录骨架全部建立；进入框架实现阶段 |
