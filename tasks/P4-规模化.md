# P4 — 规模化

- 状态：**进行中**
- 开始日期：2026-09-18
- 完成日期：

---

## 目标

在规范已验证的前提下，**按学科批量填满工具集**，并通过 SEO 与内容运营把流量池做大。

> 原则：**框架稳、组件齐之后才扩量**。P2 只做 3-5 个，本阶段才允许"批量生产"。

---

## 前置条件

- [x] P3 完成（分发链路就绪，能承接流量）
- [x] `_shared/` 组件稳定（`et-import` / `et-chrome` / 公共 logic 已冻结）
- [x] P2 的规范验证结论已回写 `docs/工具开发规范.md`
- [ ] 后台统计看板有真实数据（用于决定优先补哪些学科）——需上线后积累

---

## 任务清单

### 一、工具生产流水线 ✅

- [x] 建立批量新增工作流：`tools/_template/` + 脚手架脚本 `scripts/new_tool.py`
  - [x] 交互式输入 id / 标题 / 学段 / 学科 / 类型
  - [x] 自动生成 `manifest.json` / `index.html`（含 `ET-META`）/ `CHANGELOG.md`
  - [x] 自动插入 `ET:INLINE` 标记并跑一次 `sync_shared.py`
  - [x] 自动跑 `check_manifest.py` 自检（外加 `check_no_external.py`）
  - [x] `--yes` 非交互模式 + `--family` / `--accent`（批量生产用）
  - [x] 模板集中在 `tools/_template/*.tpl`（`@@NAME@@` 占位，避开 CSS `%` 与 JS `{}`）
- [x] 工具 id 命名规范固化（kebab-case，`family` 关联变体）
- [x] 单工具体积守卫：HTML > `TOOL_MAX_KB`（默认 500KB）→ 告警（提示例外清单）

### 二、按学科填满

> 📋 **执行计划见 [`P4-工具批次计划.md`](P4-工具批次计划.md)**（依据 `KeeTools_工具清单.md` 拆成 6 批，
> 每批 7–8 个，含 id / type / 学段 / 学科 / 要点与每批 DoD）。

按学段 × 学科矩阵推进，优先**高频课堂场景**：

- [x] 通用（跨学科）：已有 5 个（点名 / 转盘 / 倒计时 / 计分 / 随机分组）
- [ ] 小学：语文 / 数学 / 英语 / 科学 / 班级管理
  - [x] 语文 3 个（拼音表 / 田字格字帖 / 听写助手）
  - [ ] 数学 3 个（已有 1：口算题生成器）
  - [ ] 英语 3 个（已有 1：字母表）
  - [ ] 科学 3 个
- [ ] 初中：语文 / 数学 / 英语 / 物理 / 化学 / 生物 / 历史 / 地理
- [ ] 高中：物理 / 化学 / 生物 / 数学
- [ ] 每个学科至少 3 个工具，形成"可打包"的最小合集
- [x] 优先复用已有 `logic/`，**只在真正需要时新增共享逻辑**（本阶段至今未新增共享片段）

### 三、内容与 SEO

- [x] 站点地图 `sitemap.xml`（首页 / 分类页 / 工具详情页，排除 `/tool/{id}/v/*` 归档）
- [x] `robots.txt`（归档版本、搜索页、后台、网盘中间页、下载端点均 `Disallow`）
- [x] 每个工具详情页 SEO 三件套：`title` / `description` / 结构化数据（`SoftwareApplication`）
  - 另加 `canonical` / `keywords` / Open Graph
- [x] 分类页文案（`CategoryCopy` 导语：学段 + 学科长尾词，可在 `site_config`
      用 `category_intro.{slug}` 覆盖）
- [x] 首页 TDK 与站点定位文案（`seo_home_title` / `site_keywords` 后台可改，留空自动生成）
- [x] 站内搜索可被索引 → `/search?q=` 一律 `noindex,follow`（`/tools` 正常收录）
- [ ] 提交站长平台（百度 / Bing / Google）——需生产域名上线后手工提交

### 四、数据分析驱动

- [x] 后台看板增加：工具数增长曲线（近 12 个月）/ 各学科工具数分布 / 零使用工具清单
- [x] **零浏览量工具下线或重做**：清单内提供「重做元数据」与「一键下架」
      （`POST /admin/tools/{id}/published`，只改站点索引，不动目录与 manifest）
- [ ] 高转化工具 → 优先做变体（`family`）/ 优先放入合集包封面位——待真实数据
- [ ] 网盘点击转化率低的工具 → 调整详情页文案与主按钮权重——待真实数据

### 五、运营配套

- [x] 公众号引流位在详情页与中间页固化（P1 已有 `community_qr_image` 配置）
- [x] 工具定制需求收集入口（`custom_tool_url` / `custom_tool_note`，详情页侧栏；
      未配置则页面零痕迹，URL 过 `Security::safeExternalUrl` 白名单）
- [x] 赞助入口（P1 已有 `/sponsor` + 页脚入口）
- [ ] 每批次工具更新后在 CHANGELOG 记录并同步 git tag——随批次执行（见批次计划 DoD）

### 六、工程健康度

- [x] git pre-commit 钩子强制跑六项校验脚本
- [ ] CI（可选，若有）：push 时跑六项 —— 见"待决策"（**结论：本阶段不引入**）
- [x] 依赖体积审计：`scripts/asset_report.py`（`public/` 汇总 + 各工具 HTML 体积排行，
      实测 public/ 162KB、工具平均 150KB、仅 `pinyin-chart` 越 500KB 红线）
- [x] 定期复查 `docs/工具例外清单.md`：已新增"体积超限登记"分区并登记 `pinyin-chart`

---

## 验收标准

1. **规模达标**：工具总数 ≥ 30，覆盖 ≥ 5 个学科，每学科 ≥ 3 个
   —— 当前 11 个工具；语文 3 ✅、通用 5 ✅；数学 1、英语 1
2. **全部合规**：`check_manifest.py` / `check_no_external.py` / `sync_shared.py --check` /
   `check_css_tokens.py` 全部退出码 0 ✅
3. **可打包**：任一学段能一键打出 < 10MB 的合集包（P3 已具备，待工具数达 30 后复测超时）
4. **SEO 就绪**：`sitemap.xml` 可访问且包含全部详情页（实测 50 条 URL）；
   归档版本 `noindex` 生效（robots + 页面级）✅
5. **数据驱动**：后台能列出"零使用工具"，并且有下线/重做记录（机制就绪，待数据）✅
6. **脚手架可用**：`python scripts/new_tool.py` 5 分钟内产出可扫描入库的新工具骨架 ✅
   （实测：生成 → 登记 → 内联 → 自检 → 入库全链路通过，产物 54KB）
7. **架构未污染**：`git status` 干净，`var/` 无残留，无临时文件进入仓库

---

## 风险与备注

| 风险 | 应对 |
|---|---|
| **批量生产导致规范漂移** | 强制走 `new_tool.py` 脚手架 + pre-commit 校验，禁止手抄复制 |
| 工具同质化（同一功能做 10 个变体） | `family` 关联；只在样式/数据/玩法确有差异时才做独立工具 |
| 共享逻辑被绕过手工内联 | `sync_shared.py --check` 进 pre-commit；发现即失败 ✅ |
| 零使用工具堆积 | 每季度清理，不看数量看使用率（看板已提供清单） |
| SEO 垃圾页（搜索页/归档页被索引） | `/search` 与 `/tool/{id}/v/*` 一律 `noindex` ✅ |
| 体积膨胀拖慢首屏 | 详情页 iframe 懒加载；静态资源走缓存头；单工具 500KB 守卫 ✅ |

### 已决策（2026-09-18 P4 收尾）

- ✅ **不引入 CI**：单人开发，发布链路是 `git push relay → WinSCP 增量上传`，CI 覆盖不到部署
  自检；本地 `pre-commit` 六项门禁 + 每批手工验收为准。有协作者时再评估。
- ✅ **工具总数软上限 = 100**：超过后维护成本（manifest 回写、体积、SEO 内容、季度复核）
  超过收益，届时改为"只优化不新增"。
- ✅ **不做"用户提交工具"入口**：V1 无用户系统（P5 才评估），且需人工审核与责任归属，
  现阶段用「工具定制需求入口」承接需求即可。
- ✅ **`hello-keetools` → 改造为 `tool-template`「工具模板示例」**（v1.0.0，`stats_enabled: false`）。

### 进度记录

| 日期 | 进展 |
|---|---|
| 2026-09-18 | P4 开工。§一 流水线完成（`_template` + `new_tool.py` 重写 + 体积守卫）；§三 SEO 完成（sitemap / robots / canonical / 分类导语 / 首页 TDK / 搜索页 noindex，实测通过）；§四 看板完成（增长曲线 / 学科分布 / 零使用工具清单 + 下架端点）；§二 首批 5 个新工具（随机分组 / 口算题生成器 / 田字格字帖 / 听写助手 / 英语字母表），工具总数 6 → 11，六项门禁全绿 |
| 2026-09-18 | P4 收尾（机制部分全部就位）：`hello-keetools` → `tool-template`「工具模板示例」；工具定制需求入口（详情页侧栏）；`scripts/asset_report.py` 体积审计；规范新增 §6.4.1「工具 HTML 禁止出现的两种字面量」（`ET:INLINE` 标记文本 / script 结束标签字面量，会被误解析/误注入）；产出 [`P4-工具批次计划.md`](P4-工具批次计划.md)（6 批 × 7–8 个，共 46 个新工具，批 3 完成即达 ≥30 工具 / ≥5 学科）；三项待决策定稿。工具总数 11 |
| — | **续（待执行）**：按 `P4-工具批次计划.md` 逐批开发工具 |

### 遗留问题

- **归档版本页 `/tool/{id}/v/*` 路由尚未实现**：当前只做了 robots 屏蔽与 sitemap 排除，
  真正需要归档旧版时再补（需求 §7 已冻结"URL 不含版本号"，归档属低频能力）。
- **验收 #1（≥30 工具）未达成**：11/30，按批次计划推进；机制与门禁已全部就位。
- **验收 #3（打包 < 10MB）复测**留待工具数达 30 后执行（P3 遗留）。
