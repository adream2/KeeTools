# P2 — 标杆工具

- 状态：**未开始**
- 开始日期：
- 完成日期：

---

## 目标

用 3-5 个真实工具**验证规范可行性**，并沉淀 `_shared/` 通用组件。

> 这一阶段的目的不是"多做工具"，而是"证明规范能干活"。规范有问题，现在改成本最低。

---

## 前置条件

- [ ] P0 完成（框架能扫描展示）
- [ ] P1 完成或并行（至少详情页可用）
- [ ] `tools/_shared/` 目录与 `sync_shared.py` 可用

---

## 计划开发的工具

| # | 工具 id | 类型 | 目的 | 复用组件 |
|---|---|---|---|---|
| 1 | `random-name-classic` | `shell` | 验证导入组件 + 大屏设计 | `et-import` / `random-pick` / `et-chrome` |
| 2 | `wheel-spinner` | `shell` | 验证动画 + Canvas/SVG 大屏渲染 | `et-import` / `et-chrome` |
| 3 | `countdown-timer` | `shell` | 验证纯逻辑共享（最简单） | `countdown` / `et-chrome` |
| 4 | `pinyin-chart` | `fixed` | 验证 fixed 型渲染器模式 | `et-chrome` |
| 5 | `scoreboard` | `shell` | 验证多状态持久化 | `scoreboard` / `et-import` |

> 若时间有限，优先 1 / 2 / 4（覆盖三类形态）。

---

## 任务清单

### 一、`_shared/` 组件沉淀（先做）

- [ ] `tools/_shared/shared.manifest.json`
- [ ] `logic/random-pick.js` — 随机抽取（去重 / 已点标记 / 加权）
- [ ] `logic/countdown.js` — 倒计时
- [ ] `logic/scoreboard.js` — 计分板
- [ ] `ui/et-import.js` + `ui/et-import.css` — **通用数据导入组件（重点基建）**
  - [ ] 粘贴文本
  - [ ] CSV / TXT 文件
  - [ ] JSON 文件
  - [ ] 编码识别（UTF-8 / GBK）
  - [ ] 自动识别列（姓名 / 学号 / 权重）
  - [ ] 去重 + 空值处理
  - [ ] 导入预览（前 10 行）
  - [ ] 错误提示（定位到行）
- [ ] `ui/et-chrome.js` — 全屏工具条 + 页脚回流条
- [ ] `icons/icons.svg` — 工具图标 symbol 集合
- [ ] `scripts/sync_shared.py` 实装完成

### 二、开发工具

- [ ] `random-name-classic`
  - [ ] `manifest.json` / `index.html` / `CHANGELOG.md`
  - [ ] 大屏字号（后排能看清）
  - [ ] 去重开关 / 已点标记 / 加权抽取
  - [ ] 全屏模式
  - [ ] localStorage 持久化
- [ ] `wheel-spinner`
  - [ ] 转盘动画（Canvas 或 SVG）
  - [ ] 支持导入奖项与权重
  - [ ] 中奖结果放大展示
- [ ] `countdown-timer`
  - [ ] 时分秒设置 / 快捷预设
  - [ ] 结束提示（视觉 + 音效可选）
  - [ ] 全屏大字
- [ ] `pinyin-chart`
  - [ ] 通用渲染器（表格 / 卡片切换）
  - [ ] 数据放独立 JSON，可替换
  - [ ] 检索 / 筛选
- [ ] `scoreboard`
  - [ ] 多队伍加分减分
  - [ ] 历史记录
  - [ ] 重置

### 三、规范验证与回写

- [ ] 逐个工具跑 `check_manifest.py` / `check_no_external.py` / `sync_shared.py --check`
- [ ] 逐个工具实测：双击 `file://` 打开可用 / 断网可用 / 1024×768 与 1920×1080 正常
- [ ] 记录规范中的**不适用点**，回写 `docs/工具开发规范.md`
- [ ] 若有工具无法单文件 → 登记 `docs/工具例外清单.md`
- [ ] 补充 `docs/需求文档-v2.md` 的工具类型定义（如发现新形态）

### 四、批量验证

- [ ] 一次性放入全部工具 → 后台扫描 → 前台全部可见
- [ ] 后台改任一工具标题 → manifest 回写正确 → git diff 可见
- [ ] 离线包打包（若 P3 已完成）验证目录结构完整

---

## 验收标准

1. **5 个工具全部可用**：双击 HTML 即用，断网可用
2. **零外部请求**：DevTools Network 面板无任何外部域名请求
3. **共享生效**：改 `_shared/logic/random-pick.js` → 跑同步脚本 → 两个工具的代码同步更新
4. **导入组件复用**：至少 3 个工具使用 `et-import`，且是同一份代码注入
5. **大屏可用**：1920×1080 全屏下，关键信息后排可辨
6. **版本一致**：`check_manifest.py` 退出码 0
7. **规范可执行**：开发过程中未出现需要违反 `docs/工具开发规范.md` 的情况，或违规点已回写文档
8. **examples**：每个工具内 `README.md`（可选）说明数据格式

---

## 风险与备注

| 风险 | 应对 |
|---|---|
| `et-import` 复杂度超预期 | 这是**核心基建**，值得投入。若 xlsx 解析过重，先砍掉 Excel 支持（CSV 覆盖 90% 场景） |
| 老浏览器 Canvas 性能 | 转盘用 SVG 或降低帧率；实测 360 浏览器 |
| 规范与实际不符 | **这正是本阶段的目的** — 发现问题立即回写文档，不要绕过 |
| 工具数量诱惑 | 严格控制在 3-5 个。**规范未稳不做第 6 个** |

### 进度记录

| 日期 | 进展 |
|---|---|
| — | — |
